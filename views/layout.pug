<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/css/style.css")
        title Challenge Wallet
    body
        h1#main_title Challenge Wallet

        if verified
            a(href="/logout").logout logout

        block content

        include footer